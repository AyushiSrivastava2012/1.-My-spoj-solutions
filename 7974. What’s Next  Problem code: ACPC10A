#include <bits/stdc++.h>
using namespace std;
 
int main() {
	long a,b,c;
	cin>>a>>b>>c;
	while(!(a==0 &&b==0 && c==0))
	{
		if((b-a)==(c-b)){
			cout<<"AP "<<(c+(b-a));
		}
		else if((b/a)==(c/b)){
			cout<<"GP "<<(c*(b/a));
		}
		cout<<endl;
		cin>>a>>b>>c;
	}
 
 
	return 0;
}
